Задача 5.
	1) Напишите метод, который вернет содержимое текущей папки в виде массива строк;
	2) Напишите метод, который запишет массив, возвращенный предыдущим методом в фаил;
	3) Обработайте ошибки с помощью try-catch конструкции. В случае возникновения исключения,
	   оно должно записаться в лог-фаил.

1)

public class Main {
	public static void main(String[] args) {
		ex5("Тут надо написать путь к файлу")
	}

	
	private static void ex5(String pathDir) {
		File file = new File(pathDir);
		if (!(file.exists() || file.isDirectory())) { // Если файла нет или файл не директория методы завершаем
			return;
		}
	}

2)

	StringBuilder sb = new StringBuilder();
	for (String filename : file.list()) {
		sb.append(fileName).append(System.lineSeparator()); // Выводим название файлов в консоль и делаем умный перенос строки с помощью System.lineSeparator()
	{
	
	System.out.println(sb);
	
3)

	try(PrintWriter pw = new PrintWriter(fileName : "Путь к фаилу")) {
		pw.print(sb);
	}
	catch (FileNotFoundException e) {
		throw new RuntimeException(e);
	}

}






















